# MySql on GitHub 2021-07-14 at 19:13:56

name: Test

on:
  push

jobs:
  python:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install
      run: |
        sudo apt-get install python3-pip 
        pip3 install mysql-connector-python

    - name: Test
      run: |
        sudo systemctl start mysql.service
        sudo mysql --user=root -e"DROP USER 'root'@'localhost'; CREATE USER 'root'@'localhost' IDENTIFIED BY '121212';  GRANT ALL PRIVILEGES ON *.* TO 'root'@'localhost' WITH GRANT OPTION;"
        python3 select.py
